syntax = "proto3";

package gameserver.common;

option csharp_namespace = "GameServer.Grpc.Common";

// 공통 응답 래퍼
message Result {
  bool success = 1;
  string message = 2;
  int32 error_code = 3;  // 내부 에러 코드 (예: "INVALID_REQUEST")
}